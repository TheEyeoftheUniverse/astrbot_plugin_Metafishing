# 执行计划：区域通行证改回按日刷新检查

## 内部执行等级

M：改动集中在 `FishingService`，数据库结构不变，只调整授权有效期和检查触发时机。

## 步骤

1. 冻结“按日刷新统一检查”需求，明确取消秒级巡检与 24 小时语义。
2. 将消耗型通行证驻留有效期改为“下一个刷新点”。
3. 新增按刷新周期执行一次的通行证统一检查入口。
4. 将每日维护入口接入通行证检查，移除自动钓鱼循环中的固定间隔巡检。
5. 做静态编译验证。

## 验证命令

- `python3 -m py_compile core/services/fishing_service.py`

## 回滚规则

- 只回滚通行证有效期与每日检查调度相关改动。
- 不回滚既有日志清理和自动钓鱼批量查询优化。

## 清理

- 不新增数据库迁移。
- 保留本次需求与计划文档。
